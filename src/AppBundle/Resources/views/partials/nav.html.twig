<!-- Fixed navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('homepage') }}">Mygavolt</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample07"
                aria-controls="navbarsExample07" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarsExample07">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('homepage') }}">
                        Home
                    </a>
                </li>

                {% if app.user %}
                    {{ render(controller('AppBundle:Nav:categories')) }}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('catalog', {'slug': 'promotions'}) }}">
                            Promotions
                        </a>
                    </li>
                {% endif %}
            </ul>
            <div class="form-inline my-2 my-md-0">
                <ul class="navbar-nav mr-auto">
                    {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('cart') }}">
                                <i class="material-icons">shopping_cart</i>
                            </a>
                        </li>

                        <li class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" id="profile" data-toggle="dropdown"
                               data-toggle="dropdown"
                               aria-haspopup="true" aria-expanded="false">
                                My account <span class="caret"></span>
                            </a>
                            <div class="dropdown-menu" aria-labelledby="profile">
                                <h6 class="dropdown-header">
                                    {{ app.user.businessName }}
                                </h6>

                                <a class="dropdown-item" href="{{ path('fos_user_profile_show') }}">
                                    Profile
                                </a>

                                <div class="dropdown-divider"></div>

                                <a class="dropdown-item" href="{{ path('fos_user_security_logout') }}">
                                    <i class="mdi mdi-logout"></i> Logout
                                </a>

                            </div>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('fos_user_security_login') }}">
                                <i class="mdi mdi-login"></i> Login
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('fos_user_registration_register') }}">
                                Register
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </div>
</nav>
